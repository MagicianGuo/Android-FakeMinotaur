<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FakeMinotaur" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="android:statusBarColor">@color/main_background</item>
        <item name="android:navigationBarColor">@color/main_background</item>
        <item name="android:windowLightNavigationBar">true</item>
    </style>

    <style name="Theme.FakeMinotaur" parent="Base.Theme.FakeMinotaur" />
</resources>